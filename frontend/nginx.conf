# Vehix Agent — frontend nginx (SPA static files only)
# API routing is handled by the internal nginx aggregator.
server {
    listen 80;
    server_name _;

    # SPA routing
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
